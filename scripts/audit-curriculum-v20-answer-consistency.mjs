// Compatibility entry point used by package scripts and CI.
// The v2 checker independently parses/recomputes every active math question
// and requires every non-math answer to be explicitly traceable in learner evidence.
import './audit-curriculum-v20-answer-consistency-v2.mjs'
