# Context listening, hints, and Pages recovery

- Full-sentence listening uses the same bilingual contextual cloze items as reading practice.
- The first help click reveals only the initial letter and word length.
- The second help click reveals the answer, marks the item as not correct, and schedules smart review.
- Pull requests run the full TypeScript, Vite, deploy-output, `404.html`, `.nojekyll`, and Pages artifact checks.
- Only `main` pushes or a manual workflow dispatch perform the final GitHub Pages deployment.
- PR #27 merged as `705284283206fe7dcf40aee77aa7f81f0f951260`.
- This documentation update intentionally records the merged release and triggers the required `main` deployment workflow after automated PR merging did not create a visible push deployment run.
